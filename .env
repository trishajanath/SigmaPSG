# This is an example environment file.
# To use, copy this file to a new file named .env and fill in your actual configuration values.
# The .env file should be added to your .gitignore to prevent committing secrets to version control.

# MongoDB connection string (replace with your actual credentials)
MONGODB_URI=mongodb://your_user:your_password@mongo:27017

# A strong, randomly generated secret key for session management, signing, etc.
# Use a tool like `openssl rand -hex 32` to generate a new key.
SECRET_KEY=your_strong_random_secret_key_here
